; Module notes for bhop_timer.amxx.
; Copy required lines into addons/amxmodx/configs/modules.ini without semicolons.

; Required for player origin checks and movement state.
fakemeta

; Required by speedrun_zone and bhop physics helpers.
engine

; Required for reliable CT team assignment.
cstrike

; Required for clean CS respawn without spectator-camera glitches.
hamsandwich

; Required by polygon zone storage.
json

; Required by speedrun_zone polygon/editor runtime.
reapi

; Required for remote records, zones, and best times.
mysql

; Optional after migration. Keep enabled for the first launch if an old
; bhop_timer SQLite database must be imported into MySQL.
sqlite
